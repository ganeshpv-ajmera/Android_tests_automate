package com.aop.interplay.player

import android.content.Context

class PlayerView constructor(
    context: Context
) : com.google.android.exoplayer2.ui.PlayerView(context) {
    // TODO: add the common methods and configure the attributes
}