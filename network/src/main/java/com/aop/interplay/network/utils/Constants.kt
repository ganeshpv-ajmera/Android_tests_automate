package com.aop.interplay.network.utils

import com.aop.interplay.network.BuildConfig


class Constants {

    companion object {
        const val BASE_URL = BuildConfig.BASE_URL
        const val APP_BASE_URL = BuildConfig.SIGN_UP_BASE_URL
        const val GET_HOME_VIDEOS = "posts"
    }
}